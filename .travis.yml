language: node_js
node_js:
  - stable

script:
  - npm test -- --coverage # Include coverage when running tests
  - npm run build
  
deploy:
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: feature